CREATE TABLE covid19(
    id SERIAL,
    country varchar(300),
    countrycode varchar(300),
    slug varchar(300),
    newconfirmed int,
    totalconfirmed int,
    newdeaths int,
    totaldeaths int,
    newrecovered int,
    totalrecovered int,
    date varchar(300),
    premium varchar(300)

)

